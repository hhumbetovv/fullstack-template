name: generator

environment:
    sdk: ^3.9.0
resolution: workspace

dependencies:
    #? Modules
    processor: any

    #! Directory and Folders
    watcher: ^1.1.1
    dart_style: ^3.1.0
    glob: ^2.1.3

    #! Analyzer
    analyzer: ^7.4.5

    #! Builders Core
    build: ^3.0.0
    source_gen: ^3.0.0
    code_builder: ^4.10.1