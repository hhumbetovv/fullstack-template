class AppDuration extends Duration {
  const AppDuration.zero() : super(seconds: 0);

  const AppDuration.visibility() : super(milliseconds: 300);

  const AppDuration.fade() : super(milliseconds: 500);

  const AppDuration.bottomNavigator() : super(milliseconds: 300);

  const AppDuration.radio() : super(milliseconds: 100);

  const AppDuration.size() : super(milliseconds: 150);

  const AppDuration.opacity() : super(milliseconds: 300);

  const AppDuration.fast() : super(milliseconds: 50);

  const AppDuration.loader() : super(milliseconds: 400);

  const AppDuration.snackBar() : super(seconds: 2);

  const AppDuration.splash() : super(seconds: 1);

  const AppDuration.pickerAddition() : super(seconds: 1);
}
