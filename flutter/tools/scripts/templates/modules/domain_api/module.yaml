name: {{module_name}}
modules:
  #! Core Modules
  - processor
  - core_domain
dev_modules:
  - gen_exporter
  - gen_data
dependencies: []
dev_dependencies:
  - build_runner

build:
  include: scripts/templates/builds/domain_api_build.yaml
  raw: true
