name: {{module_name}}
modules:
  #! Core Modules
  - processor
  - core_domain
dev_modules:
  - gen_exporter
  - gen_data
dependencies:
  # Dependency Injection
  - injectable
dev_dependencies:
  - build_runner
  # Dependency Injection
  - injectable_generator

build:
  include: templates/builds/domain_build.yaml
  raw: true
