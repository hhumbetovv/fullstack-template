name: {{module_name}}
modules:
  - core_domain
  - core_data
dev_modules: 
  - gen_exporter
dependencies:
  # Serialization
  - json_annotation
dev_dependencies:
  - build_runner
  # Serialization
  - json_serializable